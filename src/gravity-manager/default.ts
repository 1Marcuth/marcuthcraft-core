import IGravityManager from "./interface"

class GravityManager extends IGravityManager {
    public update(): void {
        // console.log("update called", Date.now())
    }
}

export default GravityManager